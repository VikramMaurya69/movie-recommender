# 🎬 Movie Recommender App

This is a Streamlit-based web app that recommends movies similar to the one you select.  
It uses a precomputed similarity matrix and movie dictionary stored in `.pkl` files.

## 🚀 How to Run Locally
1. Install dependencies:
   ```bash
   pip install -r requirements.txt
